[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.19"  # Ensure this matches the version in your Solidity contracts

# [default]
# solc_version = "0.8.19"  # Ensure this matches the version in your Solidity contracts

[profile.release]
evm_version = "berlin"  # Specify a compatible EVM version

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options
